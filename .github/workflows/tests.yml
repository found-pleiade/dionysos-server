---
name: Testing Routes

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  launch-tests:
    runs-on: ubuntu-latest
    with:
      context: .
    steps:
      - name: Run Test Command
        run: docker-compose -f docker-compose_testing.yaml up --build -d && docker-compose exec api go test -v tests/*